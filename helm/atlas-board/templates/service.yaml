apiVersion: v1
kind: Service

metadata:
  name: atlas-board-service
  namespace: {{ .Release.namespace  }}

spec:
  selector:
    app: atlas-board

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

  type: {{ .Values.service.type }}